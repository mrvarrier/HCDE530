{
  "version": 2,
  "buildCommand": "cd api && npm install && npx playwright install chromium --with-deps && cd .. && npm install && npm run build",
  "outputDirectory": "dist",
  "functions": {
    "api/**/*.js": {
      "memory": 3008,
      "maxDuration": 60
    }
  },
  "routes": [
    {
      "src": "/mp2/(.*)",
      "dest": "/$1"
    }
  ]
}
